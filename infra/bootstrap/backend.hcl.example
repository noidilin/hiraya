# Copy to backend.hcl after the first local bootstrap apply creates this bucket.
# Do not commit backend.hcl; it is intentionally account-specific.

bucket       = "devops-hiraya-dev-tf-state"
key          = "devops-hiraya-dev/bootstrap/terraform.tfstate"
region       = "ap-northeast-1"
use_lockfile = true
encrypt      = true
