apiVersion: v1
kind: Service
metadata:
  name: vintage-postgres
  namespace: vintage
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
